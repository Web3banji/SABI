import {
  UserProfile,
  WorkRecord,
  EvidenceItem,
  ClientConfirmation,
} from '../types';

// Clean state: No formulated or mock data
export const SEED_DISCOVER_USERS: UserProfile[] = [];
export const SEED_DISCOVER_WORK_RECORDS: WorkRecord[] = [];
export const SEED_DISCOVER_EVIDENCE: EvidenceItem[] = [];
export const SEED_DISCOVER_CONFIRMATIONS: ClientConfirmation[] = [];
